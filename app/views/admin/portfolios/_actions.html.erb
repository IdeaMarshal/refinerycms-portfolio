<ul>
  <% if Admin::PortfoliosController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_portfolios_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_portfolio_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::PortfoliosController.sortable? and Portfolio.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Portfolios"),
                 admin_portfolios_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Portfolios"),
                 admin_portfolios_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>